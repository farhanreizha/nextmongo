const NavList = ({ children }) => (
  <li className='px-4 py-2 font-bold text-white bg-blue-500 rounded-full hover:bg-blue-700 transition ease-in-out duration-500 cursor-pointer'>
    {children}
  </li>
)

export default NavList
